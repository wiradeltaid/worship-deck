# _bmad-output

WDI Method workspace layer for transient, in-progress skill outputs.

Per WDI Method layer boundaries (`CLAUDE.md` / `AGENTS.md`):
- **Purpose:** Work in progress; committed, not curated.
- **Rule:** A file in this directory does not hold authority. What survives its wave passes into the corpus (`.what/`, `.how/`, `.control/`), and completed specifications reside under `.archive/specs/`.
- **Status:** Empty. Historical prior knowledge and early wave specifications (W1–W11) have been retired and archived into `.archive/specs/`.
