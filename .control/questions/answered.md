# Answered

**Loaded when:** looking for a decision already taken through a question, not through a `DEC-`.

Rows move here from the other three lists. MUST NOT be deleted.

## Answered

| id | Question | Answer | Closed |
|---|---|---|---|
| OQ-24 | Is Registry `gone` terminal — does Reset ever undelete? | Yes, terminal. `deleteSongSetEntry` (`internal/httpapi/song_set_entries.go:338-417`) hard-deletes and records a tombstone; no undelete/restore path exists anywhere in `internal/`. `src/lib/registry/store.ts:455-477` throws `RegistryNotFoundError` on a missing row. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-15 | Does Reset restore the shipped label including a rename, with an authored row exposing no Reset? | Yes, confirmed as-built and unchanged. `internal/httpapi/registry.go:672-694` and `store.ts:92,455-477` — Reset is rejected unless `seed_hash` is non-empty (authored rows have none); when allowed it overwrites label/payload unconditionally, including a prior rename. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-14 | Does a stale snapshot get any extra operator affordance beyond the Admin Sync button? | No — confirmed still "nothing extra," as chosen. `SyncArtifactButton.tsx:44-54` is a plain button with only success/error toasts; no staleness badge or diff view exists anywhere in `src/operator`. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-32 | When Sync Artifact ships, is a corrupt live Registry row omitted and logged rather than frozen into the snapshot? | Yes, confirmed. `internal/httpapi/registry.go:800-846` (`syncArtifact`) and `src/lib/registry/service-snapshot.ts:54-117` both skip (log + `continue`) on a parse/serialization failure rather than writing the row into `service_registry_snapshots`. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-5 | Does PPTX remain the offline guarantee, with the browser slideshow best-effort only? | Yes, and this is also the spine's own binding rule (AD-1) independent of this question. `SlideshowPage.tsx:15-30` is network-dependent with no offline cache; PPTX remains the only offline artifact. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-20 | Are Generate and Preview exempt from BR-4's stale-precondition, since they are not a Hub payload edit? | Yes, confirmed. `previewService` (`internal/httpapi/services.go:1063`) and `getPptx` (`server.go:208-248`) contain no `UPDATE services` — already marked verified in `SDD-hub.md`. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-23 | After a rejected save, does a gone Service 404 rather than recreate, and does session expiry reject with no partial write? | Yes, confirmed. 404-on-`sql.ErrNoRows` path in `services.go`; session gate runs before handlers (`server.go:143`); `deleteService`'s transaction only commits when `RowsAffected > 0` — already marked verified in `SDD-hub.md`. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-33 | Does `PUT /api/announcements` with `items: []` total-replace and wipe recurring rows with no confirm? | Moot — the entire `/api/announcements` route family is retired (DEC-004/FR-3). `server.go` registers zero `/api/announcements*` routes. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-34 | Are Announcement PUT/PATCH last-write-wins, exempt from AD-6? | Moot — same retirement as OQ-33; the resource no longer exists. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-42 | Does the Telegram webhook still accept `announcements[]` and write `announcement_items` after FR-3's retirement? | No, confirmed closed by `internal/httpapi/webhook_test.go::TestWebhook_AnnouncementsIgnoredNoAnnouncementItemsWritten`, which asserts zero `announcement_items` rows are written by the webhook post-retirement. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-21 | Does an unreadable date refuse to create a Service on both the Hub form and the Telegram webhook path? | No — only the Hub form path refuses (`create-service.ts`). The Telegram webhook (`internal/httpapi/webhook.go:52-56`) falls back to today's date via `parse.LocalISODate(time.Now())` and creates the Service anyway. This asymmetry is a real, already-tracked gap: `SDD-hub.md` row 174 marks it `[MISSING]`. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-22 | Do Telegram images attach or fail visibly, rather than being silently dropped? | No — `src/lib/images.ts`'s `coerceImageUrls` silently filters unsafe/non-string entries with no per-URL failure surfaced anywhere. Already tracked as `[MISSING]` in `SDD-hub.md` row 176. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-26 | Does plan identity travel on presenter shared-state messages, do empty verse reference and lookup timeout fail closed, does no-projector refuse a lookup, and does a missing plan return to Hub? | Split. Two of four clauses confirmed true and closed here: plan identity now travels on every shared-state message (`src/lib/present-channel.ts` — `planIdentity` on `sync`/`blank`/`transition`/`background`/`scripture`/`clear-scripture`); empty verse reference fails closed (`PresenterOperator.tsx:1257` client, `internal/httpapi/scripture.go:16-19` server). Two clauses remain genuinely open, reopened with the exact still-missing behavior named as **OQ-60**: lookup-timeout-fails-closed (no `AbortController`/timeout exists anywhere in `pushScripture` or the server query) and no-projector-refuses-lookup (`pushScripture` never checks liveness before broadcasting). The fourth clause, "missing plan returns to Hub," turned out narrower than stated — `PresentPage.tsx` redirects only on a missing Service (404), not on a present Service with an empty plan — folded into OQ-60's restated wording rather than closed as true. | 2026-09-23 agent (via wdi-reconcile) |
| OQ-7 | Must deleting a Service (FR-10) also delete files in `UPLOADS_DIR` and the PPTX cache, or is the SQLite row enough (photos/prayers could remain on disk)? | Yes: deleting a Service deletes local files in `UPLOADS_DIR` bound to that Service. Recurring announcement items (and their files) remain (BR-5). The PPTX cache is not part of this answer. | 2026-08-18 kodesh87 |
| OQ-1 | Events will keep sending Rundowns in a parseable form like today. | Parked on CAP-11 (Telegram last phase). Hub intake (FR-27) is the current path; the Operator entering the form is no longer the failure mode. | 2026-08-19 kodesh87 |
| OQ-9 | Is Registry delete/reorder today via the store, rather than DELETE/reorder HTTP on LC-11? | Neither. `store.ts` exports list/get/update/reset/insertIfMissing only. Admin delete/reorder HTTP is [MISSING] until FR-21 / UC-15. AD-17 non-revival is verified by SQL delete in `tests/registry-reseed.test.mjs`, not by a store method. | 2026-08-19 G4 Registry pass |
| OQ-11 | Does linear advance during blank shift the Deck index? | Yes. Blank itself does not change index (BR-6 / FR-16). Advance while blanked still moves the index; the projector stays black; unblank shows the current index. As-built `PresenterOperator.setIndexAndSync`. | 2026-08-19 G4 Presenter review |
| OQ-6 | The G3 portrait is as-built one `web` container (AD-2/AD-4). The Go+SPA direction in the brief addendum has not yet reversed an AD. | Void. Owner 2026-08-19 treats Go+SPA as binding. DEC-003 accepted; AD-30 plus AD-2/4/5/9/24 amendments. | 2026-08-19 kodesh87 |
| OQ-38 | AD-36's bootstrap-only install model does not cover `POST /api/admin/song-books` (`05-song-books.md`) creating an admin-invented `book_code` with no corpus file; until an owner decision says otherwise, that route is assumed out of scope for v1 and MUST NOT ship. | Allowed. Owner (live session, 2026-08-20): an Admin-created song book with no corpus file behind it is allowed — the menu for it does not exist yet so nothing ships now, but the capability is intended for the future (an Admin adds their own book and fills it in one hymn at a time; a later bulk JSON import is the likely companion feature, recorded in the spine's Deferred ledger, not designed here). Ruled an **extension** of AD-36, not a reversal — AD-36's Rule only describes the corpus-bootstrap path and never forecloses another creation path, so the gap is filled rather than contradicted. AD-36 amended in place with a new bullet naming what an admin-created book is (a `song_books` row created with its bootstrap marker set and zero `hymns` rows, filled only by later authoring) and the corpus-collision answer (setting the marker at creation means a same-coded corpus file arriving later is inert for that code, per AD-36's existing "never re-seeds an already-marked code" rule — no special-case needed). `05-song-books.md`'s POST row and Constraints updated to match. | 2026-08-20 kodesh87 (via wdi-question) |
| OQ-39 | An admin-created song book (AD-36 extension, OQ-38) has no corpus file to declare AD-26's locale/licence/provenance from. Assumed: locale required at creation (Admin-supplied, load-bearing per AD-26); licence and provenance optional/nullable — nothing else in the system depends on them being present for a song book. Routes to `.how/registry/02-contracts/05-song-books.md` (LC-11, POST) once ruled. | Settled, not reversed. Owner (live session, 2026-08-20): for an admin-created song book, locale, licence and provenance are all simply filled in by the Admin — the same posture `book_code` and `name` already had at creation. AD-26 amended (AD-36's Extended clause, 2026-08-20): "that row still carries this rule's full shape in exactly one piece — code, name, locale, licence, provenance, the same five fields as any other song book — sourced from the Admin at creation instead of a file's declared metadata." The ruling names *who* supplies the three fields; it does not touch OQ-39's original required/optional split, which the ruling leaves undisturbed: locale stays required at creation (load-bearing per AD-26 — every corpus row carries a locale, admin-created or not), licence and provenance stay optional/nullable (nothing downstream depends on their presence for a song book, corpus-backed or admin-created). `05-song-books.md`'s POST/PATCH bodies and Validation lane updated to carry all three fields for an admin-created book. | 2026-08-20 kodesh87 (via wdi-question) |
| OQ-41 | Underline stays unavailable on the Registry canvas. `textDecoration` is absent from `allowedStyleKeys` in `internal/plan/validate_artifact.go`, so a control for it could not survive a save; bold and italic shipped, underline did not. | Confirmed still true by manual QA 2026-09-08 (`.work/requirements/prompt-04-artifacts-main-spine-qa-followup.md` § TOOLBAR ELEMENT PROPERTIES #3) and filed as **BUG-10** in `defects.yaml`, targeted for wave W12 — add `textDecoration` to `allowedStyleKeys` and both render paths, plus the toolbar button. | 2026-09-08 agent (via wdi-decision pipeline) |
| OQ-59 | The "Slide was modified by another session... discarded" concurrency banner seen in one Announcement Set QA screenshot (Image #10) — is it an incidental race from the owner's own test session, or reproducible? | Reproducible, and it is a single session, not two tabs. Owner (2026-09-08): the two stacked title blocks in BUG-9 had identical content; the owner edited/saved the TOP block, then tried to edit the BOTTOM block, and that second edit produced this exact banner. Folded into **BUG-9** in `defects.yaml` as its load-bearing clue, not tracked as a separate item — same defect, not a coincidence. | 2026-09-08 kodesh87 |
| OQ-57 | The Live Slide Preview does not show the badge vocabulary the owner asked for: it renders `Song Set` for a song-set group header and `lyric` for every lyric child, where the owner asked for `song-set-1` / `song-set-2` and the child's lyric role (`bait 1`, `reff`). | Fixed, and the cause recorded here was only half of it. (a) `SlidePreviewList` rendered a hardcoded `Song Set` literal as the group badge and dropped the `groupOrdinal` it had already computed; the badge now comes from `resolveSongSetGroupBadge`, with `ordinalBadge` the single place `song-set-N` / `ann-set-N` is built. (b) The lyric role label never reached the preview at all — not "an empty `slide.title`" as recorded, but because `/api/services/preview` returns `plan.DrawItem` while the SPA types it as `SlidePreviewItem`, two shapes sharing no field, so every `slide` arriving at `resolvePreviewBadge` was empty. The label is now carried on the group child (`GroupRef.RoleLabel`, set from `lyric.Label` in both plan builders) rather than scraped from the resolved layout — a `Reff` and a `Chorus` section both collapse into `layoutKey: "reff"` and neither sets `verse_number`, so a scrape could never have recovered which one it was. The acceptance fixture proves the point: those rows resolve to `chorus`. The residual prop mismatch is carried forward as OQ-58. Guards: `allowedBadgePattern` lost `lagu|khotbah|ayat` and `song set`; because `lyric` stays a legal badge the closed-vocabulary check alone cannot fail on a lost label, so the fixture now also asserts no badge in it is `lyric`. Both guards were observed to fail on an injected defect and the injection reverted. | 2026-08-22 agent (via wdi-question) |
